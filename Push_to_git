How to Push file on Github

Kindly note that From August 13, 2021, GitHub is no longer accepting account passwords when authenticating Git operations.
You need to add a PAT (Personal Access Token) instead, and you can follow the below method to add a PAT on your system.

Create Personal Access Token on GitHub
From your GitHub account, go to Settings => Developer Settings => Personal Access Token => Generate New Token (Give your password) 
=> Fillup the form => click Generate token => Copy the generated Token, it will be something like ghp_sFhFsSHhTzMDreGRLjmks4Tzuzgthdvfsrta



1. Create Personal Access Token on GitHub
From your GitHub account, go to Settings => Developer Settings => Personal Access Token => Generate New Token (Give your password) 
=> Fillup the form => click Generate token => Copy the generated Token, it will be something like ghp_sFhFsSHhTzMDreGRLjmks4Tzuzgthdvfsrta

2. Copy the repo url on which you want to push the files
3. Open your terminal and clone the directory with command: git clone 'your_repo_url'
4. Now on the terminal same foler is created
5. Now move all files or folders which you want to push on that repor to that directory
6. now run below commands:
    - git add .   --here '.' means to upload all the files and folder
    - git status    --This will show the list of all the files you added with above command
    - git commit -m 'your message'   --here -m stands for message
    - git push -u origin master/main   --Choose the origin as per your repor master or main
7. HEre in push it will ask for user name and password so in username provide username and in password provide that token you have 
   generated
